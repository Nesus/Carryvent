Eventos Publicados

<% @eventos.each do |evento| %>
	<div>
		Titulo:
		<%= evento.name %>
		Descripcion
		<%= evento.subtitle %>
		<%= evento.information %>
		<%= evento.longitude %>
		<%= evento.latitude %>
		<%=link_to "Editar", editar_evento_path(evento) %>
	</div><br>
<% end %>
